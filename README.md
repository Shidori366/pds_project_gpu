A horrible GPU implementation of segmented "Sieve of Eratosthenes" algorithm. ROCm was used.

Params like block size, number of threads and segment size are set for RX 7700 XT which was used during development.
